<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Document</title>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form text-white">
            <form class="p-10" action="/create" method="post">
                <input class="block px-3 py-2 bg-zinc-800 w-full rounded-md outline-none" type="text" name="title" placeholder="Title goes Here">
                <textarea class="block px-3 py-2 bg-zinc-800 resize-none mt-2 w-full rounded-md outline-none" name="details" id="" placeholder="Write your task details here"></textarea>
                <input class=" mt-2 bg-blue-600 px-5 py-2 rounded-md" type="submit" value="Create Task">
            </form>
            <div class="tasks p-10 flex gap-3 flex-wrap">
                <% if (files.length > 0) { %>
                    <% files.forEach((val) => { %>
                        <div class="task min-w-72 px-3 py-4 bg-zinc-800 rounded-md">
                            <h1 class="text-white text-3xl tracking-tighter"><%= val %></h1>
                            <a class="text-[#94a3b8] inline-block" href="/files/<%=val%>">Read More</a>
                        </div>
                    <% }); %>
                <% } else { %>
                    <h3 class="text-zinc-600">No tasks Yet ...</h3>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>